# GPIO stack: the e-Paper driver (resources/waveshare_epd/epdconfig.py) uses gpiozero,
# not RPi.GPIO. On Bookworm+ gpiozero needs the lgpio backend. RPi.GPIO removed (unused,
# non-functional on Pi 5).
gpiozero
lgpio
spidev==3.5
# ponytail: versions below are the stale 2024 pins. Refresh + re-pin via `pip freeze`
# on the target Pi OS Python (PRD §9 step 2 / P1-2) — must run on the Pi, not this dev box.
Pillow==9.4.0
numpy==2.1.3
rich==13.9.4
pandas==2.2.3
netifaces==0.11.0
ping3==4.0.8
get-mac==0.9.2
paramiko==3.5.0
smbprotocol==1.14.0
pysmb==1.2.10
pymysql==1.1.1
sqlalchemy==2.0.36
python-nmap==0.7.1