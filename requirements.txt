# GPIO stack: the e-Paper driver (resources/waveshare_epd/epdconfig.py) uses gpiozero,
# not RPi.GPIO. On Bookworm+ gpiozero needs the lgpio backend. RPi.GPIO removed (unused,
# non-functional on Pi 5).
gpiozero
lgpio
spidev==3.5
# Web server stack (webapp v3 — replaces the stdlib http.server/socketserver implementation).
# uvicorn[standard] pulls in websockets (needed for /ws/stats) and uvloop where available.
# python-multipart is required by FastAPI's UploadFile (used by the /restore endpoint).
fastapi==0.140.13
uvicorn[standard]==0.51.0
python-multipart==0.0.32
# ponytail: versions below are the stale 2024 pins. Refresh + re-pin via `pip freeze`
# on the target Pi OS Python (PRD §9 step 2 / P1-2) — must run on the Pi, not this dev box.
Pillow==9.4.0
numpy==2.1.3
rich==13.9.4
# pandas==2.2.3 removed 2026-08-14: nothing imports it since 2579fc3 dropped the last three
# lazy-imports for stdlib csv. It was 50-80 MB and a 2-5s import on a 512 MB Pi Zero, installed
# on every device for code that no longer existed. numpy stays — the Waveshare e-Paper driver
# needs it.
netifaces==0.11.0
ping3==4.0.8
get-mac==0.9.2
paramiko==3.5.0
smbprotocol==1.14.0
pysmb==1.2.10
pymysql==1.1.1
sqlalchemy==2.0.36
python-nmap==0.7.1